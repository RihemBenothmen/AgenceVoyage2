{% extends 'base.html.twig' %}

{% block title %}Voyage{% endblock %}

{% block section %}
 <div style="background-color: #427D9D; width: 100%; height: 100px; "></div>
 <div class="row g-4 mb-4">
				    <div class="col-12 ">
				        <div class="app-card app-card-stats-table h-100 shadow-sm">
					        <div class="app-card-header p-3">
						        <div class="row justify-content-between align-items-center">
                                <table>
                                <tr>
                                  <td>
							       <div class="col-auto text-center"> 
                                      <img src="{{ asset(voyage.image) }}" alt="" class="img-thumbnail">
                                   </div>
                                  </td>
                                  <td>
                                   <div class="col-auto">
                                     <h1 class="">Description :</h1>{{ voyage.description }}
                                     <h1 class="">Prix : {{ voyage.prix }} DT </h1><br>
                                       <div class="">
                                         <hr>
                                          <div class="d-flex justify-content-center mb-2">
                                             <a href="{{ path('app_booking', {'id': voyage.id}) }}" class="btn btn-sm btn-primary px-3" style="border-radius: 30px 30px 30px 30px;">Book Now</a>
                                             </div>
                                         </div>
                                  </div>
                                 </td>
                                 </tr>
                                 </table>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}