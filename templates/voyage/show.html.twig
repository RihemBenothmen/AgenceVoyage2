{% extends 'admin.html.twig' %}

{% block title %}Voyage{% endblock %}

{% block section %}

 <div class="row g-4 mb-4">
				    <div class="col-12 ">
				        <div class="app-card app-card-stats-table h-100 shadow-sm">
					        <div class="app-card-header p-3">
						        <div class="row justify-content-between align-items-center">
                                <table>
                                <tr>
                                  <td>
							       <div class="col-auto"> 
                                      <img src="{{ asset(voyage.image) }}" alt="" class="img-thumbnail">
                                   </div>
                                  </td>
                                  <td>
                                   <div class="col-auto">
                                     <h1 class=" text-center">{{ voyage.titre }}</h1>
                                     <h1 class="app-card-title">Description :</h1>{{ voyage.description }}
                                     <h1 class="app-card-title">Prix :</h1> {{ voyage.prix }} DT <br>
                                       <div class="text-center">
                                         <a class="badge bg-success" href="{{ path('app_voyage_index') }}">back to list</a>
                                         <a class="badge bg-warning" href="{{ path('app_voyage_edit', {'id': voyage.id}) }}">edit</a>
                                         <hr>
                                         {{ include('voyage/_delete_form.html.twig') }}
                                         </div>
                                  </div>
                                 </td>
                                 </tr>
                                 </table>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
