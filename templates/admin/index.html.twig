{% extends 'admin.html.twig' %}

{% block title %}Admin{% endblock %}

{% block section %}

 <div class="row g-4 mb-4">
				    <div class="col-6 col-lg-3">
					    <div class="app-card app-card-stat shadow-sm h-100">
						    <div class="app-card-body p-3 p-lg-4">
							    <h4 class="stats-type mb-1">Total reservations </h4>
							    <div class="stats-figure">{{nbreservation}}</div>
							    
						    </div><!--//app-card-body-->
						    <a class="app-card-link-mask" href="#"></a>
					    </div><!--//app-card-->
				    </div><!--//col-->
				    
				    <div class="col-6 col-lg-3">
					    <div class="app-card app-card-stat shadow-sm h-100">
						    <div class="app-card-body p-3 p-lg-4">
							    <h4 class="stats-type mb-1">Total voyages</h4>
							    <div class="stats-figure">{{nbvoyage}}</div>
							    
						    </div><!--//app-card-body-->
						    <a class="app-card-link-mask" href="#"></a>
					    </div><!--//app-card-->
				    </div><!--//col-->
				    <div class="col-6 col-lg-3">
					    <div class="app-card app-card-stat shadow-sm h-100">
						    <div class="app-card-body p-3 p-lg-4">
							    <h4 class="stats-type mb-1">--</h4>
							    <div class="stats-figure">--</div>
						    </div><!--//app-card-body-->
						    <a class="app-card-link-mask" href="#"></a>
					    </div><!--//app-card-->
				    </div><!--//col-->
				    <div class="col-6 col-lg-3">
					    <div class="app-card app-card-stat shadow-sm h-100">
						    <div class="app-card-body p-3 p-lg-4">
							    <h4 class="stats-type mb-1">--</h4>
							    <div class="stats-figure">--</div>
						    </div><!--//app-card-body-->
						    <a class="app-card-link-mask" href="#"></a>
					    </div><!--//app-card-->
				    </div><!--//col-->
			    </div><!--//row-->
			     <div class="row g-4 mb-4">
				    <div class="col-12 col-lg-6">
				        <div class="app-card app-card-stats-table h-100 shadow-sm">
					        <div class="app-card-header p-3">
						        <div class="row justify-content-between align-items-center">
							        <div class="col-auto">
						                <h4 class="app-card-title">Les voyages</h4>
							        </div><!--//col-->
						        </div><!--//row-->
					         </div><!--//app-card-header-->
					         <div class="app-card-body p-3 p-lg-4">
    <table class="table">
        <thead>
            <tr>
                <th class="meta  stat-cell">ID</th>
                <th class="meta  stat-cell">TITRE</th>
                <th class="meta  stat-cell">PRIX</th>
                <th class="meta  stat-cell">ACTIONS</th>
            </tr>
        </thead>
        <tbody>
        {% for voyage in voyages %}
            <tr>
                <td class="meta  stat-cell">{{ voyage.id }}</td>
                <td class="meta  stat-cell">{{ voyage.titre }}</td>
                <td class="meta  stat-cell">{{ voyage.prix }}</td>
                <td class="meta  stat-cell">
                    <a class="badge bg-success" href="{{ path('app_voyage_show', {'id': voyage.id}) }}">show</a>
                    <a class="badge bg-warning" href="{{ path('app_voyage_edit', {'id': voyage.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
					        </div><!--//app-card-body-->
				        </div><!--//app-card-->
			        </div><!--//col-->

			        <div class="col-12 col-lg-6">
				        <div class="app-card app-card-chart h-100 shadow-sm">
					        <div class="app-card-header p-3">
						        <div class="row justify-content-between align-items-center">
							        <div class="col-auto">
						                <h4 class="app-card-title">Les reservations</h4>
							        </div><!--//col-->
						        </div><!--//row-->
					        </div><!--//app-card-header-->
					        <div class="app-card-body p-3 p-lg-4">
							    <div class="mb-3 d-flex">   
							        <select class="form-select form-select-sm ms-auto d-inline-flex w-auto">
									    <option value="1" selected>This week</option>
									    <option value="2">Today</option>
									    <option value="3">This Month</option>
									    <option value="3">This Year</option>
									</select>
							    </div>
					<table class="table">
        <thead>
            <tr>
                <th class="meta  stat-cell">ID</th>
                <th class="meta  stat-cell">DATE</th>
                <th class="meta  stat-cell">VOYAGE</th>
                <th class="meta  stat-cell">Montant</th>
                <th class="meta  stat-cell">ACTIONS</th>

            </tr>
        </thead>
        <tbody>
        {% for reservation in reservations|slice(0, 5) %}
            <tr>
                <td class="meta  stat-cell">{{ reservation.id }}</td>
                <td class="meta  stat-cell">{{ reservation.date | date('Y-m-d') }}</td>
				<td class="meta  stat-cell">{{ reservation.voyage.titre }}</td>
				<td class="meta  stat-cell">{{ reservation.montant }}</td>
                <td>
                    <a class="badge bg-success" href="{{ path('app_reservation_show', {'id': reservation.id}) }}">show</a>
                    <a class="badge bg-warning" href="{{ path('app_reservation_edit', {'id': reservation.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
	<div class="navigation">
{{ knp_pagination_render(reservations) }}
 </div>
					        </div><!--//app-card-body-->
				        </div><!--//app-card-->
			        </div><!--//col-->
			    </div><!--//row-->

				
			    <div class="row g-4 mb-4">
				    <div class="col-12 ">
				        <div class="app-card app-card-stats-table h-100 shadow-sm">
					        <div class="app-card-header p-3">
						        <div class="row justify-content-between align-items-center">
							        <div class="col-auto">
						                <h4 class="app-card-title">Les utilisateurs</h4>
                                    </div>
									<br><br>
<hr>
<table class="table">
        <thead>
            <tr>
                <th class="meta  stat-cell">ID</th>
                <th class="meta stat-cell">EMAIL</th>
                <th class="meta stat-cell">NOM</th>
                <th class="meta stat-cell">PRENOM</th>
                <th class="meta stat-cell">TELEPHONE</th>
                <th class="meta stat-cell">ACTIONS</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
		 {% if  user.nom != "admin" %}
            <tr>
                <td class="stat-cell">{{ user.id }}</td>
                <td class="stat-cell">{{ user.email }}</td>
                <td class="stat-cell">{{ user.nom }}</td>
                <td class="stat-cell">{{ user.prenom }}</td>
                <td class="stat-cell">{{ user.telephone }}</td>
                <td class="stat-cell">
                    <a class="badge bg-success" href="{{ path('app_user_show', {'id': user.id}) }}">show</a>
                    <a class="badge bg-warning" href="{{ path('app_user_edit', {'id': user.id}) }}">edit</a>
                </td>
            </tr>
			  {% endif %}
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
							        </div><!--//col-->
						        </div><!--//row-->
					        </div><!--//app-card-header-->
					        <div class="app-card-body">
		
					        </div><!--//app-card-body-->
				        </div><!--//app-card-->
			        </div><!--//col-->
			        
			    </div><!--//row-->
    {% endblock %}